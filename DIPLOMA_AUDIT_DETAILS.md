# Точечный аудит диплома против vers2

Источник диплома: `bd3dc15d-_______________1.docx` (идентичен `0c9d27b6-...`, размер совпадает).
Эталон спецификации: `TABLES_vers2.md` §4.4 (41 модуль).
Реальное состояние кода: `find src/main/java/ru/autotestgen -name "*.java"` → 41 .java-файла.

> **Важно по терминологии.** В дипломе таблиц с «модулями» две: **Табл. 92** в §1.5.4 (стр. ≈ 2127 docx, «Спецификация модулей программы» — 35 строк) и **Табл. П3.1** в **Приложении 3** (стр. ≈ 3293 docx, «Спецификация на разработанную программную документацию и программное обеспечение» — 35 строк). Пользователь упоминает «Прил. 3», ниже разобраны **обе** таблицы.

---

### 1. Прил. 3 (модули)

#### 1.1. Расхождение по количеству

| Источник                                  | Кол-во модулей | Откуда взяли                                                                        |
| ----------------------------------------- | -------------- | ----------------------------------------------------------------------------------- |
| Диплом, §1.5.4, Табл. 92                  | **35**         | Прямой подсчёт строк «.java» в табл. 92 (включая `CliMain`, `CliRunner`, `TestCaseRow`) |
| Диплом, Приложение 3, Табл. П3.1          | **34** (+ `main.fxml`) | Подсчёт «.java» (включая `CliMain`, `CliRunner`; **без** `TestCaseRow`)            |
| Реальный код (только .java)               | **41**         | `find src/main/java/ru/autotestgen -name "*.java" \| wc -l`                          |
| **TABLES_vers2.md §4.4 (эталон)**         | **41**         | Прямой подсчёт строк таблицы 4.1                                                    |

> Утверждение пользователя «должно быть 28» **не подтверждается** ни кодом, ни эталоном `TABLES_vers2.md` — реально 41 .java-файла (если учитывать всё), а в эталонной табл. 4.1 — те же 41 модуля. Если требуется именно 28, нужно явно определить, какие классы свернуть в «один модуль» (например, объединить весь пакет `model` в «Объектная модель»). Текущий разбор ведётся на **41 модуль ≈ эталону**.

#### 1.2. Расхождения по составу — Табл. 92 (§1.5.4)

| Действие         | Модуль                       | Причина                                                                              |
| ---------------- | ---------------------------- | ------------------------------------------------------------------------------------ |
| **УДАЛИТЬ**      | `CliMain.java`               | Класса нет в коде vers2 (нет файла `cli/CliMain.java`).                              |
| **УДАЛИТЬ**      | `CliRunner.java`             | Класса нет в коде vers2.                                                              |
| **УДАЛИТЬ** или вынести как примечание | `TestCaseRow.java`           | Это **внутренний static class** `MainController.TestCaseRow`, отдельного файла нет. Не должен быть строкой в таблице модулей. |
| **ДОБАВИТЬ**     | `EntityParser.java`          | Новый класс пакета parser (декомпозиция XmlModelParser).                              |
| **ДОБАВИТЬ**     | `PropertyGroupParser.java`   | Новый класс пакета parser.                                                            |
| **ДОБАВИТЬ**     | `SearchParser.java`          | Новый класс пакета parser.                                                            |
| **ДОБАВИТЬ**     | `StaxUtils.java`             | Новый класс пакета parser (утилиты StAX).                                            |
| **ДОБАВИТЬ**     | `XmlNamespaces.java`         | Новый класс пакета parser (константы NS_E, NS_E3, NS_MD).                            |
| **ДОБАВИТЬ**     | `DatabaseConnection.java`    | Новый класс пакета data (управление JDBC-соединением).                                |
| **ДОБАВИТЬ**     | `SchemaInitializer.java`     | Новый класс пакета data (DDL).                                                        |
| **ДОБАВИТЬ**     | `TestRunDao.java`            | Новый класс пакета data (DAO таблицы test_run).                                       |
| **ДОБАВИТЬ**     | `TestCaseDao.java`           | Новый класс пакета data (DAO таблицы test_case).                                       |
| **ИСПРАВИТЬ**    | `SearchResultProperty. java` | В дипломе **пробел** в имени файла («Property. java»). Должно быть `SearchResultProperty.java`. |
| **ИСПРАВИТЬ**    | `Модульная структура системы включает 32 класса` (стр. 2120) | По факту **41** (без inner-классов). Указать новое число. |

#### 1.3. Расхождения по составу — Табл. П3.1 (Приложение 3)

| Действие     | Модуль                     | Причина                                                                |
| ------------ | -------------------------- | ---------------------------------------------------------------------- |
| **УДАЛИТЬ**  | `CliMain.java`             | Нет файла в vers2.                                                      |
| **УДАЛИТЬ**  | `CliRunner.java`           | Нет файла в vers2.                                                      |
| **ДОБАВИТЬ** | `EntityParser.java`        | Новый.                                                                  |
| **ДОБАВИТЬ** | `PropertyGroupParser.java` | Новый.                                                                  |
| **ДОБАВИТЬ** | `SearchParser.java`        | Новый.                                                                  |
| **ДОБАВИТЬ** | `StaxUtils.java`           | Новый.                                                                  |
| **ДОБАВИТЬ** | `XmlNamespaces.java`       | Новый.                                                                  |
| **ДОБАВИТЬ** | `DatabaseConnection.java`  | Новый.                                                                  |
| **ДОБАВИТЬ** | `SchemaInitializer.java`   | Новый.                                                                  |
| **ДОБАВИТЬ** | `TestRunDao.java`          | Новый.                                                                  |
| **ДОБАВИТЬ** | `TestCaseDao.java`         | Новый.                                                                  |
| **СВЕРИТЬ**  | `ReportDao.java` (описание)| Сейчас «Доступ к локальной БД с историей прогонов»; теперь это **фасад** над connection + 2 DAO + SchemaInitializer. |

#### 1.4. Прочие моменты в табл. 97 (§1.7.4 «Тестирование модулей»)

| Дата       | Модуль в дипломе          | Проблема                                                                                                    |
| ---------- | ------------------------- | ----------------------------------------------------------------------------------------------------------- |
| 10.04.2026 | `ReportService.java`      | **Класса нет.** В vers2 фасад называется `ReportDao` (фасад поверх `DatabaseConnection`+`TestRunDao`+`TestCaseDao`+`SchemaInitializer`). Заменить на `ReportDao.java`. |
| 11.04.2026 | `CliRunner.java`          | **Класса нет.** Удалить строку «консольный режим выполняет полный цикл без GUI» — CLI больше не поддерживается. |

---

### 2. Раздел 1.7 (тестирование)

#### 2.1. Расхождения в 1.7.2.1 «Требования к функциональным характеристикам»

| Было в дипломе                                                  | Должно стать                                                                                       |
| --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| «JavaFX 24»                                                     | **Проверить** реальную версию в `pom.xml` — указано `JavaFX`, но конкретная версия в требованиях должна совпадать с `<javafx.version>` (есть риск рассогласования). |
| Бул-список без упоминания: уровней тестов SMOKE/BASIC/FULL       | Добавить: «выбор уровня тестов SMOKE / BASIC / FULL с разным набором генерируемых методов»          |
| Бул-список без упоминания: режима fastMode                       | Добавить: «быстрый режим прогона с `-DforkCount=3 -Dheadless=true` (3 параллельных headless-Chrome)» |
| Бул-список без упоминания: HTML-отчёта                          | Добавить: «генерация HTML-отчёта v5 со скриншотами по шагам и CSV-выгрузки»                          |
| «консольный режим работы без графического интерфейса» **(стр. 2309)** | **Удалить** — CLI больше нет в vers2 (нет `CliMain`/`CliRunner`).                                  |
| «запуск через mvn test с тайм-аутом 10 минут»                   | **Сверить тайм-аут** — в коде `TestRunner` фактический тайм-аут может отличаться (надо проверить, сейчас зашит `ProcessBuilder` без явного `waitFor(10, MINUTES)`). |

#### 2.2. Что отсутствует в 1.7.3 «Состав, порядок и методы испытаний»

| Что должно быть, но нет                                              | Куда добавить |
| -------------------------------------------------------------------- | ------------- |
| Описание видов тестов: `testFieldsPresent`, `testCreate`, `testCreateOnlyRequired`, `testUpdate`, `testDelete`, `testLogicalEdit`, `testArchive`, `testRequiredFieldValidation`, `testPartialRequiredFieldValidation`, `testSearch*`, `testGridColumns` | Новый под-абзац после «методов функционального тестирования» |
| Привязка набора методов к уровню: SMOKE = только `testFieldsPresent`; BASIC добавляет `testSearch*`+`testGridColumns`; FULL добавляет валидационные + CRUD | Новый под-абзац |
| Стек запуска: **Selenium 4.15.0, JUnit 5.10.1, WebDriverManager 5.6.2, Surefire 3.2.2** | Под-абзац о технологическом стеке |
| Отдельно: artefact-производство — HTML v5 со скриншотами + CSV + SQLite-история через `ReportDao` (фасад) | Под-абзац о фиксации результатов |

#### 2.3. Расхождения в 1.7.4 «Результаты проведения испытаний», табл. 97

| Строка диплома              | Реальность                                                                                  | Правка                                                                                                       |
| --------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `ReportService.java`        | Класса нет; фасад — `ReportDao`                                                              | Заменить имя на `ReportDao.java`. Описание оставить, но привязать к новой архитектуре (Connection+DDL+2 DAO). |
| `CliRunner.java`            | Класса нет                                                                                   | Удалить строку.                                                                                              |
| Отсутствуют                  | `EntityParser`, `PropertyGroupParser`, `SearchParser`, `StaxUtils`, `XmlNamespaces`, `DatabaseConnection`, `SchemaInitializer`, `TestRunDao`, `TestCaseDao` | Добавить хотя бы по одной строке тестирования на каждый новый класс.                                          |
| «генерация и запуск тестов» | Реально есть **3 уровня тестов** + **fastMode**                                              | Добавить тест-кейсы: «прогон SMOKE-уровня», «прогон BASIC-уровня», «прогон FULL-уровня», «fastMode 3×forkCount». |
| «формирование отчёта entity-classification.csv» (табл. 98, 11.04.2026) | Файла `entity-classification.csv` в коде нет; есть HTML-отчёт v5 + CSV прогона через `RunReportWriter.writeCsv`. | Заменить требование на «формирование HTML-отчёта со скриншотами + CSV-выгрузки прогона». |

---

### 3. Раздел 1.6 (интерфейс) — кратко

Сверка с `src/main/resources/fxml/main.fxml`:

| Элемент в дипломе (табл. 95)                  | В main.fxml? | Комментарий                                                                 |
| --------------------------------------------- | ------------ | --------------------------------------------------------------------------- |
| `xmlPathField`, `btnSelectXml`, `urlField`, `loginField`, `passwordField`, `outputDirField`, `btnSelectOutputDir`, `siteTypeCombo`, `subsystemField`, `btnParse`, `btnGenerate`, `btnRunTests`, `btnShowHistory`, `entityListView`, `resultsTable`, `statusLabel`, `progressBar`, `logArea` | да | Все 18 элементов есть.                                                       |
| `testLevelCombo` (ComboBox SMOKE/BASIC/FULL)  | **нет в дипломе** | Реально присутствует в `main.fxml` (строка 40). **Добавить в табл. 95.**     |
| `smokeAllSubsystemsCheck` (CheckBox)          | **нет в дипломе** | Реально присутствует (строка 41). **Добавить.**                              |
| `fastModeCheck` (CheckBox «Быстрый режим (3×headless)») | **нет в дипломе** | Реально присутствует (строка 48). **Добавить.**                              |
| `btnRunSelected` (Button «Выбрать тесты...»)  | **нет в дипломе** | Реально присутствует (строка 46). **Добавить.**                              |
| `totalLabel`, `passedLabel`, `failedLabel`    | **нет в дипломе** | Счётчики «Всего/Успешно/Ошибки» (строки 71–73). **Добавить.**                |
| `siteTypeCombo` тип «E3Core / generic / custom» | да           | Соответствует.                                                              |
| `subsystemField`                              | не виден в main.fxml | **Проверить:** в FXML не найден; может быть инициализируется только в коде. Перепроверить в `MainController.java`. |

Граф диалога (§1.6.1) и его состояния **в целом соответствуют** реальным сценариям, но:

| Состояние | Замечание                                                                                       |
| --------- | ----------------------------------------------------------------------------------------------- |
| S6 «Тесты выполняются» | Нужно добавить вариант «параллельный режим (fastMode = 3 headless-Chrome)».                 |
| Новое состояние | S5a «Выбран поднабор тестов» — диалог `onRunSelected` со списком чекбоксов сущностей + видов тестов (`buildTestFilter`). Сейчас в графе не отражён. |

Колонки таблицы результатов (табл. 96) — соответствует main.fxml: `colClass`, `colMethod`, `colStatus`, `colDuration`, `colMessage`. Единственное: в дипломе статусы перечислены как «PASSED / FAILED / SKIPPED», а в коде (`MainController.TestCaseRow`, см. `TABLES_vers2.md` §2.2) — `OK / FAIL / SKIP`. Унифицировать.

---

## Сводка действий по разделам

| Раздел      | Кол-во правок | Приоритет |
| ----------- | ------------- | --------- |
| §1.5.4 Табл. 92      | 12 (3 удалить + 9 добавить + опечатки)        | Высокий   |
| Прил. 3 Табл. П3.1    | 11 (2 удалить + 9 добавить + 1 описание)      | Высокий   |
| §1.7.2.1             | 5 (4 добавить + 1 удалить про CLI)             | Средний   |
| §1.7.3               | 4 (добавить виды тестов, уровни, стек, отчёты) | Средний   |
| §1.7.4 Табл. 97      | 3 (имя ReportDao, удалить CliRunner, новые модули) | Высокий   |
| §1.6.2 Табл. 95      | 6 элементов добавить                           | Средний   |
| §1.6.1 граф диалога  | 2 (S5a + параллельный режим в S6)              | Низкий    |

**Ключевая структурная правка:** во всём документе заменить упоминания CLI-режима, `CliMain`, `CliRunner`, `ReportService` на актуальную архитектуру vers2 (пакет `data` из 5 классов, пакет `parser` из 6 классов, только GUI).
