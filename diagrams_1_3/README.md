# Раздел 1.3 — анализ процесса обработки информации, методы и алгоритмы (расширенная версия)

Текст, схемы и таблицы построены **по фактическому коду программы** AutoTestGenerator
(пакеты `ru.autotestgen.*`), руководству оператора платформы **E3Core** и руководству
пользователя тестируемой **АИС «Гаражно-строительные кооперативы»**. Раздел описывает
**логику** обработки данных и выбранные методы/алгоритмы; классы и методы исходного
кода здесь не приводятся (их проектирование — в разделах 1.4–1.5). Объём — порядка
20 страниц: 11 рисунков и 13 таблиц с содержательной информацией.

**Схемы алгоритмов выполнены по ГОСТ 19.701-90 (ИСО 5807-85)**: терминатор (овал) —
начало/конец; процесс — прямоугольник; решение — ромб; предопределённый процесс —
прямоугольник с двумя вертикальными линиями; данные — параллелограмм. Все схемы ч/б.

## Рисунки

| Файл | № | Содержание |
|------|---|-----------|
| `ris_1_pipeline.png`       | 1  | Конвейер обработки информации |
| `ris_browser.png`          | 2  | Трёхпанельный браузер объектов E3Core |
| `ris_domain.png`           | 3  | Доменная модель сущностей АИС ГСК (композиция/ссылки) |
| `ris_2_xml_structure.png`  | 4  | Структура XML-метамодели E3Core |
| `ris_3_xml_fragment.png`   | 5  | Фрагмент входной XML-модели |
| `ris_4_stax_parse.png`     | 6  | Алгоритм потокового разбора XML (StAX) |
| `ris_5_classification.png` | 7  | Алгоритм классификации сущностей |
| `ris_6_page_object.png`    | 8  | Паттерн Page Object |
| `ris_7_generation.png`     | 9  | Алгоритм генерации тестового проекта |
| `ris_8_test_data.png`      | 10 | Алгоритм подбора тестовых данных (компактный) |
| `ris_9_run_analyze.png`    | 11 | Алгоритм запуска тестов и анализа результатов |

> Имена файлов сохраняют прежние номера; номер рисунка в отчёте определяется
> порядком вставки в `build_docx.py`.

## Таблицы (13)

входные/выходные данные · типы групп свойств E3Core · сущности АИС ГСК · элементы
XML-метамодели · сравнение DOM/SAX/StAX · категории сущностей · классификация
сущностей АИС ГСК · состав тест-проекта · примеры транслитерации · правила подбора
данных · грамматика масок · состав проверок · артефакты отчётности.

## Форматирование

Текст: Times New Roman 14, межстрочный 1,5, красная строка 0,75 см, по ширине.
Рисунки и подписи — по центру (подпись 14). Таблицы — 12 пт, межстрочный 1, по левому краю.

## Воспроизведение

```bash
cd scripts
python3 gen_graphviz.py   # все png-схемы (ГОСТ) + фрагмент XML + браузер + домен
python3 build_docx.py     # Раздел_1.3_исправленный.docx
```
